name: cooking-notes

services:
  site:
    build:
      context: .
    init: true
    ports:
      - "${SITE_PORT:-4324}:4321"
    environment:
      CHOKIDAR_USEPOLLING: "true"
    volumes:
      - .:/app
      - node_modules:/app/node_modules

volumes:
  node_modules:
